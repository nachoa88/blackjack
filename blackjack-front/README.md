# Blackjack front end
Utilitzat Docker amb la configuració detallada al `docker-compose.yml` i `Dockerfile`, estic desenvolupant el front end per consumir la API rest creada en Laravel (blackjack-api).

### Vue3 JS - Tailwind CSS - Axios
## Why Axios
1) Auth.
Since your API has authentication, roles, and permissions, Axios is often simpler to maintain thanks to interceptors and built-in features for headers and error handling.

2) Error and Exception handling.

## Why Tailwind?
1) Maybe i'll explain later.

## Testing!!!